import React from 'react'
import {
  Text,
  View
} from 'react-native'
import MainView from '../../components/MainView'

export default NewsPage = ({navigation}) => {

  return <MainView>
    <Text>
      news
    </Text>
  </MainView>
}