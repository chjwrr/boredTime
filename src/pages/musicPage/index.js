import React from 'react'
import {
  Text,
  View
} from 'react-native'
import MainView from '../../components/MainView'

export default MusicPage = ({navigation}) => {

  return <MainView>
    <Text>
      music
    </Text>
  </MainView>
}