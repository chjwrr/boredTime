import React from 'react'
import {
  Text,
  View
} from 'react-native'
import MainView from '../../components/MainView'

export default GamePage = ({navigation}) => {

  return <MainView>
    <Text>
      game
    </Text>
  </MainView>
}